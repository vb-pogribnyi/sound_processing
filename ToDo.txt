--------------------------- GENERAL ------------------------------------

1. - Profile generation - make number of timesteps parameter; the timestep itself is to be calculated. 
2. + Export meta information, such as timestep and mesh steps
3. + When generating h5 file - remove those mesh parts that contain no signal (p_mask should be ones only if the mesh contains signal)
4. + Make number of timesteps between checkpoints a parameter as well (As final sampling rate)
5. + Verify that the wave propagates with the speed of sound
6. + Add GPU implementation of turbulent noise (sines). That way, the exported profile image will act as filtering surface.
7. + Export WAV sound
8. + For profile generation - be more clear about the dimientions of the box.
9. + Make CUDA app output multiple frames at different time, so that it's not required to re-start so often.
10. - Include source speed, so that doppler effect is applied.
11. + Use tmpfs (ram-based) for generating inputs into kwave
12. Make sure that the frequency is correct and dimensions are right.
13. + Add density and sound speed variations
14. Weird shifts at certain moments?
15. If a ckpt exists but the .bak does not - discard the checkpoint and start over.
16. Calculate whether to record the first time step in a correct way (currently it's checking if the output is empty.
17. Faster generation of profile meshes

--------------------------- USABILITY ------------------------------------

1. + Create folder for every experiment.
2. + Read from config file.
3. + Allow multiple meshes at configurable locations
4. + Configure the masked sine signal
5. + Export GIFs for configurable slices.
6. Export mics data at configurable locations.
7. + Allow source meshes with different number of time points
8. Use separate thread or process for the GIF generation
9. Load the NPZ inputs simultaneously in different threads
10. Load the inputs for the next step in advance.
11. + While exporting audio data, write only indexes in the arrays, populate afterwards
12. Create debug configuration where the density would change dramatically at some position - to debug the exported data.
13. In the exported debug image draw the energy (or frequency) of the received signal - something that indicates higher density.
