{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clear_kwave",
            "type": "shell",
            "command": "rm -rf /app/processing && rm -rf /app/outputs && rm -rf /inputs/processing"
        },
        {
            "label": "clear_slices",
            "type": "shell",
            "command": "rm -rf /app/slices"
        }
    ]
}