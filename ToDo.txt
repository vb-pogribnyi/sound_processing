+ When exporting dataset - add microphones coordinates as well
+ Fix paths for MMAUD dataset, make it export into data/mmaud
Add microphones configuration json for the MMAUD
Export sample rate and signal length
+ Export source location along with (euler) angles
+ Set time start/end to exprt in mmaud
+ Set up sensor rotations 
+ Save SRP images for each rotations
+ Integrate TAME into the universal train pipeline

For MMAUD testing - add option to test all microphones rotations
+ Add option to test the captured recordings
+ Add visualization of the final prediction
+ Make visualization of the inputs (srp & spec) more systematic
+ Add description for exporting the MMAUD dataset
+ Add root folder description (that points to the dataset exporter thing)
+ Implement validation routine in the train pipeline
+ Save trained models
+ Ensure training with different combinations of model/preprocessing is possible (e.g. Cross3d with spectrogram & Conformer with SRP)
+ Fix the 0.02m grid source
+ Make Cross3D work with both spectrogram & srp
+ Integrate mlflow into the training

- Set up main USB transfer back
- Keep the FPGA buffer un-interrupted
- Make the DB flexible enough to handle multiple channels (16, 32)
- OV camera jpeg display
- Overlay menus
- SRP, Beamforming software
- Loading list of coordinates in QR code

Figure out why TAME performs worse than reported in papers
    - Try different windows with unchanged collected
    - Create issue on Github